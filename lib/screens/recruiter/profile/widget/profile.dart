import 'package:flutter/material.dart';

class MyRecruiterProfileBody extends StatelessWidget {
  const MyRecruiterProfileBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
